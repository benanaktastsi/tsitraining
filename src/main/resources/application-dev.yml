spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 1234
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/tsi
    username: root
  jpa:
    hibernate:
      ddl-auto: create
